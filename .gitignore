# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
.eggs/

# Virtual environment
.venv/
venv/
env/

# Data files (large parquet / CSV — kept locally only)
data/

# State runtime files (trades, heartbeat) — live on Railway persistent volume
# BUT: strategy.yaml and goal.yaml are committed (they are config, not secrets)
state/*/trades.jsonl
state/*/heartbeat.json
state/*/hypotheses.jsonl
state/history/

# Secrets
.env
*.env.*

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test artefacts
.pytest_cache/
charts/*.png
charts/*.html
